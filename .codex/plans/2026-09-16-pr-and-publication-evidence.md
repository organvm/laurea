# PR and publication evidence

Owner: Codex, continuation of the full gap-filling plan in Laurea PR #10.

Bounded PR observations bind immutable base repository ID and head/base SHAs,
inspect at most five PRs within the existing 40-request/60-second reader, and
re-read generations. Draft/conflict blockers are positive observations; checks,
review counts and mergeability never substitute for required policy or owning
acceptance evidence. Omitted, denied, malformed and moved observations stay
unmeasured. Reviewer decisions follow documented API chronological order.

A read-only live probe at 2026-09-16T03:19:08Z found public repository 1289397231
at default faf6c5f67130925518233f5f99a76182b544fb85. PR #10 then had head
588c60e6192c83f1fb74f012f4d549ff03270111, two failing check results, no current-head
approval, and stable head/base. This was not executed-code failure proof or merge
acceptance. Required policy remained unmeasured.

## Review dispositions

- 4021848568 and the organization-discovery outside-diff finding: incomplete or
  malformed coverage now prevents all report/arena/verdict publication before
  existing files or same-day history change. No new measured-zero fallback.
- 4021848572: membership discovery is included in attempted source counts.
- 4021848577: methodology now describes complete pagination and failure scope.
- 4021848581: identity-free corpus aggregates preserve token-visible non-fork,
  language and star semantics across detectors, hero, arena and verdict while
  private repository rows remain excluded. No public-only silent redefinition.
- 4021852407: direct profile formatting normalizes non-mapping coverage; command
  publication separately rejects incomplete or malformed coverage.
- 4021889071: failed or malformed job reads keep aggregate verification unknown.
  The same rule applies to PR detail coverage.

Regression tests cover private aggregate propagation, previous asset/history
preservation, malformed coverage, generation movement, denied reads, incomplete
connections, stale reviews, detail caps, and optional check failures. Use
`PYTHONPATH=src python3 -m pytest -q`: the ambient editable install points at a
different checkout and is not evidence for this tree.

Next: bind required workflow/security and acceptance policy before claiming full
health; integrate the evidence into published snapshots without hiding unknown
scope. Preserve the explicit administered-estate denominator separately from
membership visibility. No current-head CI admission or deployment is inferred.

Validation: 72 tests passed with the explicit source path; git diff --check passed.
