# Show HN draft — withdrawn pending truth-safe rewrite

Do not publish the former percentile pitch. LAVREA currently supports only a
bounded GitHub activity snapshot with explicit field definitions and provenance.
A new launch draft may describe those measured fields after the current release
and hosted examples are verified.
