# Social thread draft — withdrawn pending truth-safe rewrite

Do not publish the former “top X%” thread. The repository no longer claims a
population rank. Any replacement must describe the exact API fields, token
visibility, and limitations recorded in `METHODOLOGY.md`.
