# X/LinkedIn thread draft

1/ Anyone can put "top 1% engineer" in a bio. I built a machine that has
to *prove* it every morning or shut up.

2/ LAVREA measures my GitHub output exactly — 26k contributions/yr,
2,268 PRs, 272 repos, 17 languages — and tiers each number against a
conservative population floor. The citation sits in the code next to
the threshold, where a PR can attack it.

3/ The headline claim is a conjunction: contribution volume AND
portfolio AND Python-led corpus AND 4+ stack layers, each clearing its
own top-1% floor. P(A∧B∧C∧D) ≤ min(legs). The claim is a floor, not a
vibe.

4/ Best part: it recomputes daily in CI. The day my output drops below
a floor, the card deletes itself from my profile. A boast with a
kill-switch is called an instrument.

5/ It already fired once: a CI token couldn't see my private orgs, the
snapshot collapsed, and the composite claim quietly removed itself.
The honesty mechanism working was the proof it's not vaporware.

6/ Zero-dependency Python, animated SVGs, MIT. Fork it, enable Actions,
get your own laurels — only the claims YOUR data supports will render.
https://github.com/4444J99/laurea
